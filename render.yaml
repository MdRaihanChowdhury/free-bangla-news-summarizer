services:
  - type: web
    name: free-bangla-news-summarizer   # <-- Blueprint Name
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
